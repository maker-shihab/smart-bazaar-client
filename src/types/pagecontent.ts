export interface PageContentProps {
  pageContent: {
    title: string;
    description: string;
  };
}
export interface Product {
  id: number;
  title: string;
  description: string;
  price: number;
  thumbnail: string;
}
